/* 当前阶段选择页 - 原语 3：picker-page（整页单选列表，选中绿色对勾，支持深色模式） */

page {
  background: var(--bg);
}

.container {
  padding: 16rpx 0 calc(env(safe-area-inset-bottom) + 40rpx);
}

/* 分组样式已合入全局 app.wxss（原语 1 .group / .group-card） */


