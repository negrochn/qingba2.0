<view class="container {{fontClass}} {{darkClass}}">

  <!-- 清空范围选择（整页单选列表，选中品牌绿对勾） -->
  <c-radio-list items="{{scopes}}" value="{{selectedKey}}"
                emptyText="暂无数据可清空" bindchange="onPick" />

</view>
